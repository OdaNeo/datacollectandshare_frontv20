/* eslint-disable no-unused-vars */
export enum stateColor {
  warning,
  success
}
export enum topicState {
  已停止,
  已启动
}
// 任务执行结果
export enum taskResult {
  失败,
  成功,
  异常
}
export enum taskResultColor {
  error,
  success,
  warning
}
